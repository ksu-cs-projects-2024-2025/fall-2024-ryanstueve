@page "{id:Guid}"
@model WebApp.Pages.AddViewPages.EditModel
@using WebApp.Models.Domain;
@{
    var message = ViewData["Message"]?.ToString();
}


<form method="post">
    <h1 class="mb-3">Edit Clothing Item</h1>

    @if(!string.IsNullOrWhiteSpace(message))
    {
        <div class="alert alert-success" role="alert">@message</div>
    }

    <div class="mb-3">
        <label class="form-label">Id</label>
        <input type="text" class="form-control" asp-for="EditClothingItemViewModel.Id" readonly/>
    </div>

    <div class="mb-3">
        <label class="form-label">Color</label>
        <--input type="text" class="form-control" asp-for="AddClothingItemRequest.Color" -- />
        <select class="form-control"
                asp-for="EditClothingItemViewModel.Color"
                asp-items="@Html.GetEnumSelectList<ClothesColor>()">
        </select>
    </div>

    <div class="mb-3">
        <label class="form-label">Type</label>
        <--input type="text" class="form-control" asp-for="EditClothingItemViewModel.Material" --/>
        <select class="form-control"
                asp-for="EditClothingItemViewModel.Material"
                asp-items="@Html.GetEnumSelectList<ClothesMaterial>()">
        </select>
    </div>


    <div class="mb-3 d-flex justify-content-between">
        <button type="submit" class="btn btn-primary" asp-page-handler="Update">Update</button>
        <button type="submit" class="btn btn-danger" asp-page-handler="Delete">Delete</button>
    </div>

</form>